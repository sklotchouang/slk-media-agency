# dependencies
/node_modules

# next.js build output
/.next
/out

# misc
.DS_Store
*.log

# local env files
.env*

# raw (unoptimized) generated art; only the optimized versions are committed
/public/generated/raw

# original static site kept as a local reference only
/legacy-site

# vercel
.vercel

# local tool settings (the shared settings and hooks ARE committed)
.claude/settings.local.json

# critique-loop screenshots (working artifacts)
/shots
